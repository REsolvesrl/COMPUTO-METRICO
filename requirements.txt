pandas==3.0.3
numpy==2.5.0
plotly==6.8.0
openpyxl==3.1.5
Pillow==12.2.0
PyMuPDF==1.28.0
opencv-python-headless==5.0.0.93
# stampa del computo in PDF: Python puro, nessuna libreria di sistema
# da installare (conta, visto che CME gira sul PC dell'utente)
reportlab==5.0.0

# pyarrow non lo usiamo direttamente: lo portava Streamlit, e pandas, se lo
# trova, ci tiene le colonne di testo. Resta fissato perche' i conti girino
# con le stesse librerie con cui sono stati provati: il 2026-08-07 la
# 25.0.0, presa da sola, e' morta di segmentation fault.
pyarrow==24.0.0

# il motore: FastAPI davanti ai moduli di sempre
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27          # solo per i test delle rotte (fastapi.testclient)
